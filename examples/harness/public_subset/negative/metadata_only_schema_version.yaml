# Negative contract fixture: metadata-only schema_version.
dsl_version: c3_strategy_v1
name: metadata-only-schema-version
kind: analysis_review_bounded_v1
schema_version: strategy_graph_spec_v1
roles:
  proposer:
    provider: codex_cli
    access: read
  critic:
    provider: claude_code
    access: read
  reviser:
    provider: codex_cli
    access: read
  auditor:
    provider: claude_code
    access: read
